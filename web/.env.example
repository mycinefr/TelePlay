# API URL (Optional)
# If not set, the frontend will use relative paths (e.g. /api/...) which works
# when served by the same origin or via Nginx proxy.
# Set this if your backend is on a different domain.
# VITE_API_BASE_URL=http://localhost:8000
